{
  "name": "kotodama-a1ry13ld",
  // src/airyield/worker.cljs を shadow-cljs (:target :esm) がコンパイルしたもの。
  // 移行前ここは SvelteKit のビルド出力 svelte/.svelte-kit/cloudflare/_worker.js を
  // 指しており、読み手が開く TypeScript はどの bundle にも入っていなかった
  // —— docs/adr/0001 を参照。
  "main": "dist/worker.js",
  "compatibility_date": "2025-03-17",
  // compatibility_flags (nodejs_compat / nodejs_als) は SvelteKit の
  // adapter-cloudflare が要求していたもので、cljs の :esm bundle には要らない。
  // 憶測で外したのではなく、workerd (`wrangler dev --local`) で全 route を
  // 叩いて確かめてから外した（docs/operator-quickstart.md）。
  "vars": {
    "APP_CAPABILITIES": "[\"publishFareClass\",\"adjustInventory\",\"fileFare\"]",
    "APP_DESCRIPTION": "air-yield — airline revenue management and pricing. Handles fare class publication, inventory control, fare filing, overbooking, group bookings, dynamic pricing, revenue reporting, and demand forecasting.",
    "APP_DISPLAY_NAME": "Air Revenue Management",
    "APP_FRAMEWORK": "cljs-esm-worker",
    "APP_NANOID": "a1ry13ld",
    "APP_PERFORMER_TYPE": "service",
    "APP_UI_TYPE": "yoro",
    "AGENTGATEWAY_MCP_ROUTER_URL": "https://mcp.etzhayyim.com/xrpc/com.etzhayyim.mcp.message"
  },
  "routes": [
    {
      "pattern": "a1ry13ld.etzhayyim.com/*",
      "zone_name": "etzhayyim.com"
    },
    {
      "pattern": "air-yield.etzhayyim.com/*",
      "zone_name": "etzhayyim.com"
    }
  ]
}
